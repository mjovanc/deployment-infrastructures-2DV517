---
- name: Create router on openstack
  hosts: localhost
  
  tasks:
    - name: Create router
      os_router:
        name: "{{router_name}}"
        network: public
        interfaces:
          - "{{subnet_name}}"
